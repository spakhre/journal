<% layout('/layout/boilerplate') %>

      <h1 class = "text-center mb-3">Your Entries</h1>
    </div>
    <% const months = ['', 'January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'] %>
    <div class="card-container d-flex flex-wrap justify-content-center">
      <% for(let entry of entries) { %>
      <div class="card" style="width: 18rem">
        <img src="img/laptopCoffee.jpeg" class="card-img-top" alt="..." />
        <div class="card-body">
          <h2 class="entry-date"><%= months[entry.month] %> <%= entry.day %> <%= entry.year %></h2>
          <h5 class="card-title"><%= entry.title %></h5>
          <p class="card-text note"><%= entry.note %></p>
          <p class="card-text tag"><%= entry.tag %></p>
          <div>
            <button
              class="btn btn-warning editButton"
              entry-id="<%= entry._id %>"
              user-id="<%= entry.userId %>"
              id="<%= entry._id %> "
              >
              <a href="/allEntries/<%= entry._id %>">Edit</a>
                            
            </button>

            <button
              class="btn btn-danger deleteButton"
              entry-id="<%= entry._id %>"
              user-id="<%= entry.userId %>"
            >
              Delete
            </button>
          </div>
        </div>
      </div>
      <% } %>
    </div>
    <script src="main.js"></script>

